<% content_for(:title, "私の恥ずかし話") %>
<section class="p-6 dark:bg-gray-100 dark:text-gray-900">
  <div class="container mx-auto flex justify-center">
    <div class="flex flex-col max-w-lg p-6 space-y-6 overflow-hidden rounded-lg shadow-md dark:bg-gray-50 dark:text-gray-800 mx-auto my-4">
      <h1 class="text-center text-2xl font-semibold mb-6">恥ずかし話</h1>
      <%= form_with model: @board, class: "new_board" do |f| %>
        <%= render 'shared/error_messages', object: @board %>
        <div class="mb-3">
          <%= f.label :title, class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_field :title, class: "mt-1 block w-full rounded-md border border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50" %>
        </div>
        <div class="mb-3">
          <%= f.label :body, class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_area :body, class: "mt-1 block w-full rounded-md border border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50", rows: 15 %>
        </div>
        <div class="mb-3">
          <%= f.label :board_image, "まだ画像は投稿できません🙇‍♀" %>
          <%= f.file_field :board_image, class: "form-control", accept: 'image/*' %>
          <%= f.hidden_field :board_image_cache %>
        </div>

        <div class="text-center">
          <%= f.submit "Be Open", class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-red-300 hover:bg-red-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-250" %>
        </div>
      <% end %>
    </div>
  </div>
</section>
